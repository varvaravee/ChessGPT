#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>
#include <libgen.h>
#include <string.h>
#include <math.h>

//Display main menu and prompt user for choice of side
int main()
{
    printf("\n Welcome to ChesGPT! The world's smartest and funniest chess program!\n\n ");
    
    //prototypes
    void mainMenu();
    int accessPiece();
    int canMove(board);
    int movePiece( Piece piece);
    board displayBoard(board);
    int checkWin(board);
    void switchPlayer();
    



    //calls mainMenu
    mainMenu();

    //***Functions***
    
    //mainMenu function
    void mainMenu()
    {
        int choice;

        // do while loop that presents menu until exit of application
        do {
            printf("Please make a selection from the main menu below.\n \n");
            printf("1. Start a game!\n");
            printf("2. Tell a joke.\n");
            printf("3. Exit.");

            scanf(" %d", &choice);
            
            
            //validate input and start game if input is 1
            if (choice == 1)
            {
                int playerSide;
                printf("\nGreat choice! Please select your preference for the color of your pieces.\n");
                printf("Input 1 for white pieces and 2 for black pieces. White pieces move first.\n");
                scanf(" %d", &playerSide);
            
                //case statements
                
                
                //assign side of chess board to player
                
                //initialize board, display to user
                
                //ask user for their move
                
                //check if 

            }

            else if (choice == 2)
            {
                printf("Knock knock!\n");
                printf("Who's there?\n");
                printf("Your mom!\n");
                printf("Now play some chess!\n");
            }
            
            else if (choice == 3)
            {
                printf("Thanks for playing! Have a great day!\n");
                printf("Exiting program.\n");
            }

            else 
            {
                printf("\nYou have selected a wrong number. You will be returned to the main menu.\n");
            }
        } while (choice != 3);
    }

    // accessPiece function
    
    //canMove function
    
    //movePiece function
    
    //displayBoard function
    
    //checkWin function
    
    //switchPlayer function
    
    

}
